@layout('layout.app')
@section('content')
@loggedIn
<p>
  Crea una nueva tira :D
</p>
@else
<div class="jumbotron bg-entiras-focus-1">
  <div class="row">
    <div class="col-md">
      <img src="https://res.cloudinary.com/entiras/image/upload/v1587691638/registrate_qjx64q.svg"
        class="mx-auto my-4 d-block img-fluid" alt="Registrate :V">
    </div>
    <div class="col-md">
      <div class="card bg-entiras-dark text-light">
        <div class="card-header">
          Registro
        </div>
        <div class="card-body">
          @include('layout.notification')
          <form action="/api/signup" method="POST">
            {{ csrfField() }}
            <div class="form-group">
              <label>
                Direcci&oacute;n de Correo
              </label>
              <input type="text" name="email" value="{{ old('email') }}"
                class="form-control {{ hasErrorFor('email') ? 'is-invalid' : '' }}"
                placeholder="ejemplo@ejemplo.com">
            </div>
            <div class="form-group">
              <label>
                Nombre de Usuario
              </label>
              <input type="text" name="username" value="{{ old('username') }}"
                class="form-control {{ hasErrorFor('username') ? 'is-invalid' : '' }}"
                placeholder="usuario">
            </div>
            <div class="form-group">
              <label>
                Contrase&ntilde;a
              </label>
              <input type="password" name="password"
                class="form-control {{ hasErrorFor('password') ? 'is-invalid' : '' }}"
                placeholder="4 caracteres m&iacute;nimo">
            </div>
            <button class="btn btn-outline-secondary btn-block" type="submit">
                Registrarse
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
@endloggedIn
<p>
  Contenido destacado...
</p>
@endsection
