@layout('layout.app')
@section('content')
@loggedIn
<p>
  Crea una nueva tira :D
</p>
@else
<div class="jumbotron">
  <div class="row">
    <div class="col-md">
      <img src="/registrate.svg" class="mx-auto my-4 d-block img-fluid" alt="Registrate :V">
    </div>
    <div class="col-md">
      <div class="card bg-entiras-dark text-light">
        <div class="card-body">
          @include('layout.notification')
          <form action="/api/signup" method="POST">
            {{ csrfField() }}
            <div class="form-group">
              <label>
                <img draggable="false" class="emoji" alt="&#x2709;&#xFE0F;"
                src="https://twemoji.maxcdn.com/v/12.1.5/72x72/2709.png">
                Direcci&oacute;n de Correo
              </label>
              <input type="text" name="email" value="{{ old('email') }}"
                class="form-control {{ hasErrorFor('email') ? 'is-invalid' : '' }}"
                placeholder="ejemplo@ejemplo.com">
            </div>
            <div class="form-group">
              <label>
                <img draggable="false" class="emoji" alt="&#x1F3F7;&#xFE0F;"
                src="https://twemoji.maxcdn.com/v/12.1.5/72x72/1f3f7.png">
                Nombre de Usuario
              </label>
              <input type="text" name="username" value="{{ old('username') }}"
                class="form-control {{ hasErrorFor('username') ? 'is-invalid' : '' }}"
                placeholder="usuario">
            </div>
            <div class="form-group">
              <label>
                <img draggable="false" class="emoji" alt="&#x1F5DD;&#xFE0F;"
                src="https://twemoji.maxcdn.com/v/12.1.5/72x72/1f5dd.png">
                Contrase&ntilde;a
              </label>
              <input type="password" name="password"
                class="form-control {{ hasErrorFor('password') ? 'is-invalid' : '' }}"
                placeholder="4 caracteres m&iacute;nimo">
            </div>
            <button class="btn btn-outline-secondary btn-block" type="submit">
                Registrarse
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
@endloggedIn
<p>
  Contenido destacado...
</p>
@endsection
